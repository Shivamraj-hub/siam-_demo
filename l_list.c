#include<stdio.h>
#include<stdlib.h>
struct node{
    int data;
    struct node*link;
};
int main(){
    struct node *head,*cnode,*pnode;
    cnode=(struct node*)malloc
    printf("Enter the data value ");
    scanf("%d",&data);
    if(cnode==NULL){
        printf("Memory is not allocated");
        break;
    }
    else{
        cnode -> data=
    }
    prinf("How many nodes you want to add\n");
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        cnode=(struct node*) (sizeof(node));
    }
    cnode=head;
    while(conde->link!=NULL){
        printf("%d",)
    }
}